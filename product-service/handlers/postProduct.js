import { Client } from 'pg';import { getCorsHeaders } from '../helpers/responseHelper.js';import { dbOptions } from '../constants/dbOptions.js';import {    SOMETHING_WENT_WRONG,    FETCH_SUCCESS,    INCORRECT_DATA,} from '../constants/responseMessages.js';const postBookQuery = `    INSERT INTO books (title, description, price, imageurl) VALUES ($1, $2, $3, $4)`;export const postProduct = async (event) => {    const { title, description, price, imageurl } = JSON.parse(event.body);    const client = new Client(dbOptions);    await client.connect();    try {        const hasInvalidParams = !description            || !title            || !imageurl            || !price;        if(hasInvalidParams) {            return {                statusCode: 400,                headers: getCorsHeaders(),                body: JSON.stringify({ message: INCORRECT_DATA })            }        }        const response = await client.query(postBookQuery, [title, description, price, imageurl]);        return {            statusCode: 200,            headers: getCorsHeaders(),            body: JSON.stringify({ message: FETCH_SUCCESS })        };    } catch (error) {        return {            statusCode: 500,            headers: getCorsHeaders(),            body: JSON.stringify({ error: SOMETHING_WENT_WRONG })        };    } finally {        client.end();    }};