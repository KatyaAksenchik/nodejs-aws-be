import { getProductsList } from '../getProductsList.js';import booksList from '../../data/booksList.json';describe('get products list handler', () => {    test('should return full list of products', async () => {        expect.assertions(1);        const result = await getProductsList();        expect(result).toEqual({            statusCode: 200,            body: JSON.stringify(booksList),        });    });});